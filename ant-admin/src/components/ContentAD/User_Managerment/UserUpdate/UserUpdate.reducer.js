const initialState  = {
    user : []
}

export const UserTableReducer = (state = initialState ,action) => {
    switch(action.type){
        case 'UPDATE' :{
            return state
        }
        default:
    }
    
}